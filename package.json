{
  "name": "root",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "dev": "cd frontend && node_modules/.bin/vite --host :: --port 8080"
  }
}
